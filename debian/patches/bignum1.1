--- a/hashable.cabal
+++ b/hashable.cabal
@@ -85,7 +85,7 @@ library
     , text        >=0.12 && <1.3
 
   if impl(ghc >=9)
-    build-depends: ghc-bignum ==1.0.* || ==1.2.*
+    build-depends: ghc-bignum >= 1.0
 
   else
     if flag(integer-gmp)
